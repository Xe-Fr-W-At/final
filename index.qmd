---
title: "Rick Mitchell"
format:
    html:
        embed-resources: true
---

I accidentally ordered twelve swiffers on amazon!

```{python}
import numpy
import matplotlib.pyplot as plt
import pandas as pd
stress = numpy.array([1,1,2,3,5,9,20])
plt.plot(stress)
plt.savefig("stress.svg")
plt.show()
strdata = pd.DataFrame([stress, [1, 2, 3, 4, 5, 6, 7]])
strdata.to_markdown()
```

This is where the actual math starts now.

```{python}

```
